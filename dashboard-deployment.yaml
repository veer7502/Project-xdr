apiVersion: v1
kind: Service
metadata:
  name: astra-xdr-dashboard
spec:
  type: NodePort
  selector:
    app: astra-xdr-dashboard
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001
      nodePort: 30081